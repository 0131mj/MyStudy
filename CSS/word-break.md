# word-break

```css
div{
    word-break: normal;
}
```

- word-break 의 기본 값은 normal 이다. 
- normal 에서 한글과 영문은 기본적으로 다르게 표기된다. 
- 언어의 설정마다 다른데, 영어는 기본적으로 keep-all, 한글은 break-all이다. 

상속되는 속성이므로, html에 쓰면 앱 전체에 먹힌다. 